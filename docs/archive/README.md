# Archive

Documents moved out of the active set in `docs/OVERVIEW.md`: superseded
proposals, and investigations whose findings are now fully reflected in
either the code or a newer document. Moved here with `git mv`, so history
is preserved; nothing in this directory is deleted, only deprioritized.

See `docs/documentation_policy.md` for the archiving rule. This directory
is currently empty — no active document has been archived yet.
